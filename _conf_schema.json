{
    "music_platform": {
        "description": "音乐平台选择",
        "hint": "选择要使用的音乐平台",
        "type": "string",
        "options": [
            "网易",
            "QQ",
            "酷我"
        ],
        "default": "网易"
    },
    "search_display_mode": {
        "description": "搜索结果展示模式",
        "hint": "选择搜索结果的展示方式",
        "type": "string",
        "options": [
            "文字",
            "图片"
        ],
        "default": "文字"
    },
    "send_song_mode": {
        "description": "发送歌曲模式",
        "hint": "选择发送歌曲的方式，卡片模式仅支持QQ平台",
        "type": "string",
        "options": [
            "卡片",
            "语音"
        ],
        "default": "卡片"
    },
    "enable_lyrics": {
        "description": "是否启用歌词",
        "hint": "开启后在发送歌曲后附加一张歌词图片",
        "type": "bool",
        "default": false
    },
    "timeout": {
        "description": "点歌操作的超时时长（秒）",
        "hint": "点歌时用户在此时间内没有进行操作则自动取消点歌",
        "type": "int",
        "default": 30
    },
    "playlist_page": {
        "description": "歌单页数",
        "hint": "搜索结果的页数",
        "type": "int",
        "default": 1
    },
    "show_song_count": {
        "description": "展示歌曲数量",
        "hint": "一次展示的歌曲数量",
        "type": "int",
        "default": 10
    }
}